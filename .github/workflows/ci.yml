name: CI

# This workflow verifies build, test, and audit on every push and pull request.
# CI success signals that the codebase compiles and passes automated checks.
# It does NOT validate interactive desktop behavior (PIN lock, workspace file
# migration, real-host install/upgrade/recovery). Those require manual
# verification on a real Windows x64 machine. See CONTRIBUTING.md.

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 22.12.0
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests and build
        run: npm run check

      - name: Audit dependencies
        run: npm audit --audit-level=high
